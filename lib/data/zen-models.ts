/**
 * Zen model data — fetched at runtime from api.zoo.network.
 *
 * All model data is now served dynamically via the API.
 * Do NOT hardcode model definitions here.
 */

export const PRICING_API = 'https://api.zoo.network/v1/pricing'
export const MODELS_API  = 'https://api.zoo.network/v1/models'
